[build]
  command = "npx tanstack-start build"
  publish = "dist/client"

[dev]
  command = "npm run dev"
  port = 3000
  targetPort = 3000

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
